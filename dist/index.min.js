async function start() { try { const e = await fetch("https://dog.ceo/api/breeds/list/all"); breedList((await e.json()).message) } catch (e) { console.log("There was a problem fetching the breed list."), alert("There was a problem fetching the breed list.") } } function breedList(e) { document.getElementById("breeds").innerHTML = `\n    <select onchange="loadByBreed(this.value)">\n        <option>Choose a dog breed</option>\n        ${Object.keys(e).map(function (e) { return `<option>${e}</option>` }).join("")}\n    </select>\n    ` } async function loadByBreed(e) { if ("Choose a dog breed" != e) { const t = await fetch(`https://dog.ceo/api/breed/${e}/images`); createImgs((await t.json()).message) } } function createImgs(e) { let t = $(".photos"), o = $("<img>"); o.attr("src", e[Math.floor(Math.random() * e.length)]), o.addClass("dogPic"), t.append(o) } start(), $(document).on("dblclick", ".dogPic", function () { $(this).remove() }), $("#breeds").on("change", "select", function () { const e = this.value; console.log(e); let t = this; setTimeout(function () { t.selectedIndex = 0 }, 800) }); let link = document.getElementById("back-to-top"); var amountScrolled = 250; window.addEventListener("scroll", function (e) { this.window.pageYOffset > amountScrolled ? link.classList.add("show") : link.className = "back-to-top" }), link.addEventListener("click", function (e) { e.preventDefault(); var t = (0 - window.pageYOffset) / 31.25; var o = setInterval(function () { window.scrollBy(0, t), window.pageYOffset <= document.body.offsetTop && clearInterval(o) }, 16) });